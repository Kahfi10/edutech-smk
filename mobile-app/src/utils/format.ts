// Format utilities - EduTech SMK
export const formatPoints = (pts: number) => + poin;
export const formatScore = (score: number | null | undefined, max = 100) => score != null ? ${score}/ : '-';
export const formatStatus = (s: string) => s.charAt(0).toUpperCase() + s.slice(1);
